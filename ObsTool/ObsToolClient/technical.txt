Modify the three numbers in objectListColumn to change the width of the object list column.

  objectListColumn: {
    flex: "0 0 100px",
    maxWidth: 100,
    minWidth: 100,
    paddingTop: theme.spacing(5),
  },


Both previews in the ImageList and the standard size of images when opened in in their dialog ResourceDialog/ResourceView
now get the images in 1000x1000 to prevent subsequent requests. To change this behavior modify these two places:

  ResourceView.tsx:
        // Sketches and jots use a stable Google Drive thumbnail URL so expanding the dialog can reuse the browser image cache.
        const driveMaxDimension = (this.state.type === "sketch" || this.state.type === "jot") ? "1000" : responsiveDriveMaxDimension;

      The default was 500x500 for maxDimension before.

  ImageList.tsx:
      // Google Drive resources use the same stable thumbnail URL in lists and dialogs so browser caching can work across views.
      const driveThumbnailDimension = (r.type === "sketch" || r.type === "jot") ? "1000" : "180";

      The default was 180x180 for thumbnailDimensions before.


The size is ultimately controlled by DsoTypeIcon’s size prop.

  For the object type icon surfaces you asked about:

  - Right-side Objects list beside Report Text in the obs session form
    Route: ObsTool/ObsToolClient/src/components/ObsSessionPage.tsx:459 -> ObsTool/ObsToolClient/src/components/ObsSessionForm.tsx
    Renderer: ObsTool/ObsToolClient/src/components/ObsSessionForm.tsx renders DsoShort for the compact object list.
    Actual size: size={22} in ObsTool/ObsToolClient/src/components/DsoShort.tsx.

  - Observed Objects tab on obs session form
    Route: ObsTool/ObsToolClient/src/components/ObsSessionPage.tsx:475 → ObsTool/ObsToolClient/src/components/
    ObservationList.tsx:36 → ObsTool/ObsToolClient/src/components/Observation.tsx:146 → ObsTool/ObsToolClient/src/components/
    DsoCard.tsx:216
    Actual size there: size={30} in DsoCard.
  - Search dropdown view
    Route: ObsTool/ObsToolClient/src/components/SearchInput.tsx:178 renders DsoCard for each autocomplete option.
    Actual size: same size={30} in ObsTool/ObsToolClient/src/components/DsoCard.tsx:216.
  - Search results page
    Route: ObsTool/ObsToolClient/src/components/SearchView.tsx:196 renders DsoCard.
    Actual size: same size={30} in ObsTool/ObsToolClient/src/components/DsoCard.tsx:216.

Icons:

- https://mui.com/material-ui/material-icons/?query=question
- https://fontawesome.com/search?q=question&ic=free-collection

Free SVG converter:

  https://svgconverter.online/